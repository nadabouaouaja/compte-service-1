apiVersion: v1
kind: Service
metadata:
  name: my-country-service
spec:
  type: NodePort
  selector:
    app: country-service
  ports:
  - port: 8086
    targetPort: 8086
    nodePort: 30081
    protocol: TCP
